const AddressesAPI = require('./AddressesAPI');
const { resolvers } = require('./resolvers');
const { typeDef } = require('./typeDef');

module.exports = {
    AddressesAPI,
    resolvers,
    typeDef
};
